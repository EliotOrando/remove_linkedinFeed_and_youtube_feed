addEventListener('DOMContentLoaded', (event) => {
  const main = document.getElementById('main');
  console.log('the main element is: ', main);
  main.remove();
  console.log('we are inside of the linkedin scrip');

})
